# Contributing

1. Before starting a new repository, apply the Biome, tsconfig, and commitlint configuration from [`dev-config`](https://github.com/newscode-dev/dev-config).
2. Follow the commitlint convention for commit messages.
3. Run `biome check` and the test suite before opening a pull request.
4. Complete the checklist in the pull request template.
